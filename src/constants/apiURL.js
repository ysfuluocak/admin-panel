const baseURL = "http://localhost:5000";
const userURL = `${baseURL}/users`;
const roleURL = `${baseURL}/roles`;
const permissionURL = `${baseURL}/permissions`;

export { baseURL, userURL, roleURL, permissionURL };
